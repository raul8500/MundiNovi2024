<% extraStyles=` `; %>

    <main style="margin-top: 10px">
        <div class="container pt-4">
            <h3 class="text-center mb-4">
                <i class="fas fa-cash-register text-primary"></i> Arqueo de efectivo
            </h3>

            <div class="table-responsive pt-3">
                <table id="tablaArqueos" class="table align-middle mb-0 bg-white">
                    <thead class="bg-light">
                        <tr class="text-center">
                            <th scope="col">Sucursal</th>
                            <th scope="col">Supervisor</th>
                            <th scope="col">Total ventas efectivo</th>
                            <th scope="col">Efectivo en caja</th>
                            <th scope="col">Diferencia</th>
                            <th scope="col">Fecha y hora</th>
                            <th scope="col">Acciones</th>

                        </tr>
                    </thead>
                    <tbody class="table-group-divider">
                        <!-- Aquí se cargarán las sucursales -->
                    </tbody>
                </table>
            </div>

        </div>
    </main>


<% extraScripts=`
    <script src="js/arqueo/efectivo/arqueoEfectivoAdmin.js"></script>
`; %>


